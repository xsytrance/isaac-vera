"""isaac-vera server seam (Spine 1 stub).

This is the ported client/server boundary from fft-psx-vera ("prime-as-brain":
the server owns parser truth; clients are thin). For the Chronicler smoke test
it only exposes the read-only facts object. No LLM, no commentary, no live feed
— those belong to Spine 2 (the Commentator).
"""
